<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Allow JIT compilation (needed by Electron's V8 engine) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Allow unsigned executable memory (required for some Electron internals) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Disable library validation so Electron's native modules load correctly -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Network client access for daemon communication -->
    <key>com.apple.security.network.client</key>
    <true/>
  </dict>
</plist>
